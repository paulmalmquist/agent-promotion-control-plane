# Release screenshots

The release process creates these files from the real production Next.js server with Playwright:

- `control-plane-overview.png`
- `blocked-candidate-detail.png`

They are intentionally not hand-authored or represented by placeholders. Capture them only after seeded data, governed copy, responsive layout, and status semantics pass review.
